// app/(app)/admin/users/page.tsx // Placeholder admin users screen (extend later).

export default function AdminUsersPage() {
  return (
    <div className="p-6">
      <div className="text-xl font-bold text-slate-900">Users</div>
      <div className="mt-2 text-sm text-slate-600">
        This is a placeholder. Add user management here (role changes, invites, etc.).
      </div>
    </div>
  );
}
