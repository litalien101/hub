// app/(app)/admin/settings/page.tsx // Placeholder admin settings screen (extend later).

export default function AdminSettingsPage() {
  return (
    <div className="p-6">
      <div className="text-xl font-bold text-slate-900">Settings</div>
      <div className="mt-2 text-sm text-slate-600">
        This is a placeholder. Add system settings here (tool list, onboarding, etc.).
      </div>
    </div>
  );
}
